apiVersion: v1
kind: Service
metadata:
  name: front-service
  labels:
    app: front-ui
    service: front-ui
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort : 80
      protocol: TCP
  selector:
    service: front-ui